- use config file, return if not exist
-- add implementation of config_file's load and save 
-- add default_config to repository
- add most strict compile rules
- send predefined inform message to ACS
- make initial inform
- get and parse ACS response
- make right .a files dependency in main Makefile
- make installation notes in readme
- add validity checks to config class constructor
- set config filename from main arg
- add preparing boost to Makefile's kind of 'prepare' scenario
- makefile to check if cmake is installed
